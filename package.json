{
  "name": "stayhard",
  "private": true,
  "version": "2.5.0",
  "type": "module",
  "description": "Stay Hard — habit tracker SPA",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch --serve",
    "build": "node esbuild.config.mjs",
    "check": "node --check src/main.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

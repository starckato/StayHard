# Environment / secrets (never commit)
.env
.env.local
.env.production
config.local.js
.stayhard-sb-pat
*.pat
scripts/.env

# Vercel / build output
.vercel
.next

# OS files
.DS_Store
Thumbs.db

# Local-only docs (spec, ops playbook, security backlog, review artifacts, prototypes)
# These contain internal process notes / example user emails / security details
# that should not be in a public repo.
document-private/
document/

# v3-only character assets remaining on disk (production uses GymRat_*)
# Files left from earlier experiments. Safe to delete manually anytime.
assets/Tier[1-6]_*
assets/tier2_*

# Local index.html snapshots (rollback safety nets)
index.html.backup-*

# esbuild bundle output (built fresh by Vercel on every deploy)
dist/

# Capacitor web-dir output (built fresh via npm run cap:prepare)
www/

# Capacitor native platform projects (re-creatable via `npx cap add ios|android`)
ios/
android/

# npm
node_modules/

# ── 2026-09-14 repo 위생 (전면 감사 후속) ──
# 세션 산출물·백업·스냅샷 — 커밋 금지
android.backup-stayhard/
.playwright-mcp/
snap1.yml
*.png.bak
cube-stack-verify.png
index-mobile-auth.png
production-*.png
trainer-light-auth.png
vercel-current.jpeg
assets/sprite_spacing_backup_362/
assets/tier4/backup_before_walk_style_unify/
assets/tier5/backup_before_reference_fix/
assets/walk_backup_before_sideview_fix/
.qrok-sb-pat
